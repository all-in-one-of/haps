/*
 * PROPRIETARY INFORMATION.  This software is proprietary to
 * Side Effects Software Inc., and is not to be reproduced,
 * transmitted, or disclosed in any way without written permission.
 *
 * COMMENTS:
 */

// Common viewport overrides.  These are the standard camera parameters.
{
    override.name	ThumbnailLight
    soho_overridefile	[ "ThumbnailLight" ]

    // Light parameter overrides
    space:world		[ $xform ]
    light_type		[ "distant" ]
    light_color		[ 1 1 1 ]
    atten_type		[ "none" ]
    light_wrangler	[ "HoudiniLight" ]
}

// Viewport overrides for VMantra
{
    override.inherit	ThumbnailLight
    override.name	VMantra
}

// Viewport overrides for RIB
{
    // This override will be used when a specific target can't be found
    override.inherit	ThumbnailLight
    override.name	RIB
}

// Viewport overrides for MI
{
    // This override will be used when a specific target can't be found
    override.inherit	ThumbnailLight
    override.name	MI
}

